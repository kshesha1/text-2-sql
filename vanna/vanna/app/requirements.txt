# ============================================================
# Conda env: Python 3.11.5
# NOTE: vanna itself is NOT installed from PyPI.
#       It is imported directly from ../src via sys.path (app/__init__.py).
#       All packages below are third-party deps vanna's legacy code
#       and our app code import at runtime.
# ============================================================

# --- Vanna core deps (declared in vanna/pyproject.toml) ---
pydantic>=2.0.0
click>=8.0.0
pandas>=2.0.0
httpx>=0.28.0
PyYAML>=6.0
plotly>=5.0.0
tabulate>=0.9.0
sqlparse>=0.4.0
sqlalchemy>=2.0.0
requests>=2.31.0

# --- pgvector (vanna legacy PG_VectorStore uses LangChain) ---
langchain-postgres>=0.0.12
langchain-core>=0.2.0
langchain-huggingface>=0.0.3
sentence-transformers>=2.7.0
psycopg[binary]>=3.1.0          # postgresql+psycopg:// driver for SQLAlchemy

# --- Vertex AI (Citi-internal gateway) ---
google-cloud-aiplatform>=1.60.0
google-auth>=2.30.0

# --- Starburst / Trino ---
pystarburst==0.12.0             # pinned to the version in your conda env

# --- Doc ingestion (PDF + PPTX) ---
pypdf>=4.0.0
python-pptx>=0.6.23

# --- Dev / REPL ---
python-dotenv>=1.0.0
