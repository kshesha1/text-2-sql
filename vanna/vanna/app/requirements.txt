# NOTE: vanna itself is NOT installed. We import it from the local clone at
# ../vanna/vanna/src by manipulating sys.path in app/__init__.py.
#
# The packages below are the third-party dependencies that vanna's legacy code
# imports at runtime (langchain-postgres, sentence-transformers, etc.) plus
# our own additions for Vertex AI, Starburst, and document ingestion.

# Vertex AI
google-cloud-aiplatform>=1.60.0
google-auth>=2.30.0

# pgvector via vanna's legacy PG_VectorStore (LangChain-backed)
langchain-postgres>=0.0.12
langchain-core>=0.2.0
langchain-huggingface>=0.0.3
sentence-transformers>=2.7.0
psycopg[binary]>=3.1.0
sqlalchemy>=2.0.0

# Starburst / Trino
pystarburst>=1.0.0
pandas>=2.0.0

# REPL / formatting
tabulate>=0.9.0
python-dotenv>=1.0.0

# Doc ingestion
pypdf>=4.0.0
python-pptx>=0.6.23
